<Window x:Class="VideoToTextApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:VideoToTextApp.ViewModels"
        mc:Ignorable="d"
        Title="Offline Video Transcriber" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Button Content="Select Video" 
                    Command="{Binding SelectVideoCommand}" 
                    Padding="10,5" 
                    Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Status}" 
                       VerticalAlignment="Center" 
                       FontWeight="Bold"
                       Foreground="#555"/>
        </StackPanel>

        <TextBox Grid.Row="2" 
                 Text="{Binding Transcript}" 
                 IsReadOnly="True" 
                 VerticalScrollBarVisibility="Auto"
                 AcceptsReturn="True"
                 FontSize="14"
                 Padding="5"/>
    </Grid>
</Window>
